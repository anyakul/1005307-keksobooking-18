'use strict';

(function () {
  var PinSize = {
    WIDTH: 70,
    HEIGHT: 50,
    RADIUS: 25,
  };

  var MapRect = {
    LEFT: 0,
    TOP: 130,
    RIGHT: 1200,
    BOTTOM: 630,
  };

  var MainPinSize = {
    WIDTH: 65,
    HEIGHT: 80,
    RADIUS: 32,
  };

  window.const = {
    MapRect: MapRect,
    MainPinSize: MainPinSize,
    PinSize: PinSize,
  };
})();
